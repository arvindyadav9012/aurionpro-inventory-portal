ISSUE 5 FIX - USER DASHBOARD ACCESS + LIVE LOCATION

1. Users with Select All Roles / allRoles now receive full View/Add/Edit/Delete access for all core reports even if older saved permission objects are incomplete.
2. Existing explicit per-report permissions remain supported.
3. Live location tracking now starts after user login and requests browser GPS permission.
4. Location is recorded periodically while the user remains logged in, not only when submitting a report.
5. Each live location record has a unique _locationId so multiple phones do not overwrite each other.
6. Worker shared-state merge now merges locations, users, stations and equipment master instead of replacing the complete arrays.
7. Existing D1 data is preserved; no reset/drop/delete operation was added.
8. Existing portal design/branding is preserved.

VERIFICATION:
- app.js syntax: PASS
- worker.js syntax: PASS
